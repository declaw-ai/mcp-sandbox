{
  "mcpServers": {
    "fetch": {
      "command": "declaw",
      "args": ["mcp", "--network-allow", "*", "--", "pip install mcp-server-fetch -q && python3 -m mcp_server_fetch"],
      "env": {}
    }
  }
}
